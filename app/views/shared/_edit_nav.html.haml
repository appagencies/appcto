%nav.subnav
  %ul
    %li{class: cp(edit_company_path(current_user.company))}
      = link_to "Company", edit_company_path(current_user.company)
    %li{class: cp(company_apps_path(current_user.company))}
      = link_to "Apps", company_apps_path(current_user.company)
