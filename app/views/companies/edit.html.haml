.edit-listing.inner
  %h1 Edit Listing: Company

  %nav
    = link_to_unless_current "Edit Company", edit_company_path(current_user.company)
    = link_to_unless_current "Add/Edit Apps", company_apps_path(@company)

  = form_for @company do |f|
    = render 'form', :f => f