.edit-listing.inner
  = render "shared/edit_nav"

  .apps
    .new.card
      = form_for [@app.company, @app] do |f|
        = render 'form', :f => f

    - @apps.each do |app|
      .app.card
        %h1= app.name
        - if can? :update, app
          = link_to "Delete App", [app.company, app], :confirm => 'Are you sure?', :method => :delete
          = link_to "Edit App", edit_company_app_path(app.company, app)