.edit-listing.inner
  %h1 Edit Listing: Apps

  = form_for [@app.company, @app] do |f|
    = render 'form', :f => f

  %h3 Delete App
  %p= link_to "Delete App", company_app_path(@app.company), :data => { :confirm => "Are you sure?" }, :method => :delete