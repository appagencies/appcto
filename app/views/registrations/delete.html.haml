= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :delete }) do |f|
  %h2 Are you sure you want to delete this product?
  %p
    = f.submit "Destroy"
    or
    = link_to "Cancel", edit_company_path(current_user.company)
